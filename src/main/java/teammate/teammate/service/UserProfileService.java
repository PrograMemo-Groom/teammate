package teammate.teammate.service;import jakarta.persistence.EntityNotFoundException;import lombok.RequiredArgsConstructor;import org.springframework.stereotype.Service;import teammate.teammate.domain.UserProfile;import teammate.teammate.repository.UserProfileRepository;@Service@RequiredArgsConstructorpublic class UserProfileService {	private final UserProfileRepository userProfileRepository;	//프로필 조회	public UserProfile getUserProfile(Long userId){		return userProfileRepository.findById(userId)				.orElseThrow(() -> new EntityNotFoundException("사용자 프로필을 찾을 수 없습니다. userId: " + userId));	}	//프로필 수정	public UserProfile updateUserProfile(Long userId, UserProfile updatedProfile){		UserProfile profile = getUserProfile(userId);		profile.setNickname(updatedProfile.getNickname());		profile.setIntroduction(updatedProfile.getIntroduction());		profile.setPreferredPosition(updatedProfile.getPreferredPosition());		profile.setStatusMessage(updatedProfile.getStatusMessage());		return userProfileRepository.save(profile);	}}